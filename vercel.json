{
  "buildCommand": "npm install && cd shared && npm install && npm run build && cd ../frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "installCommand": "npm install --production=false"
}